(word 'a 'b 'c) 
(se 'a 'b 'c)

;the first takes each element and puts them together in a word (no spaces)
;the second takes each element and puts them together in a sentence (with spaces between each element)
