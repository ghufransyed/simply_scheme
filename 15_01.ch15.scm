(define (to-binary num)
  (if (equal? (1 (count num)))
    )

#|Approach: consider modulo 2, put result in position one (2^0)
 | then modulo num - 1 , put result in position two etc
 | consider recursion
 |
 |
 |
 |
 | |#
