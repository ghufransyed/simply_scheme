(bf 'x)
(butfirst '(x))

;the first gives all 'letters' of the word except the first
;as there is only one letter, it returns the empty string

;the second gives all words in the sentence except the first
;as there is only one 'word', 'x' in the sentence, it returns
;an empty sentence (an empty list)